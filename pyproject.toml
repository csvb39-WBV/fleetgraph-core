[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fleetgraph-core"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
	"fastapi>=0.110,<1.0",
	"httpx>=0.27,<1.0",
	"uvicorn>=0.30,<1.0",
]
